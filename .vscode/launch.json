{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch current Go file",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${file}"
        }
    ]
}
